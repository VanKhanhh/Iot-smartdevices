import pandas as pd
from sklearn.linear_model import LinearRegression
import joblib

df = pd.read_csv("moisture_data.csv") 

X = df[['current']]
y = df['next']

model = LinearRegression()
model.fit(X, y)

joblib.dump(model, 'moisture_predictor.pkl')
print("Trained model ML.")
